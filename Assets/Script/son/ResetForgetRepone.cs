using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ResetForgetRepone
{
  // Start is called before the first frame update
      public ResetForgetRepone(bool status)
      {
        this.status = status;
      }

      public bool status;
}
